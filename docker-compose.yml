services:
  nodepower:
    build: .
    ports:
      - "8787:8787"
    environment:
      PORT: "8787"
      PUBLIC_BASE_URL: "http://localhost:8787"
      DATA_DIR: "/app/data"
    volumes:
      - nodepower-data:/app/data

volumes:
  nodepower-data:
